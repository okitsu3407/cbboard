<%= form_with(model: grate, local: true) do |form| %>
  <% if grate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grate.errors.count, "error") %> prohibited this grate from being saved:</h2>

      <ul>
      <% grate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :学年 %>
    <%= form.text_field :gakunen, id: :grate_gakunen %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
